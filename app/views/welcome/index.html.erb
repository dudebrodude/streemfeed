   <% if user_signed_in? %>

<%= render 'usercp' %>
  
  <% else %>

  <%= render 'devise/sessions/new' %>

<% end %>





<% if user_signed_in? %>
<div class="col-sm-6">
  <div class="holder">
    <h4 class="headline">Featured</h4>
  </div>
</div>
<% else %>
<div class="col-sm-8">
  <div class="holder">

  
  <h3 class="holder-head"> New to Streemfeed?</h3>

  </div>
</div>
<% end %>
<% if user_signed_in? %>
<div class="col-sm-3">
  <div class="holder">
    Test 
  </div>
</div>
<% end %>


