	<div class="col-sm-3">
		<div class="holder">
     <% if user_signed_in? %>
     <h4 class="headline"><%= fa_icon 'home' %> <%= current_user.fname %></h4>
     <table>
      <tbody>
        <tr>
          <td><h5 class="menu-option"> <%= fa_icon 'envelope' %> <%= link_to 'Messages', edit_user_registration_path, :class => 'navbar-link' %></h5></td>
        </tr>
        <tr>
          <td><h5> <%= fa_icon 'photo' %> <%= link_to 'Photos', edit_user_registration_path %></h5></td>
        </tr>
        <tr>
          <td><h5> <%= fa_icon 'child' %> <%= link_to 'Friends', edit_user_registration_path %></h5></td>
        </tr>
        <tr>
          <td><h5> <%= fa_icon 'edit' %> <%= link_to 'Edit your account', edit_user_registration_path %></h5></td>
        </tr>
      </tbody>
    </table>
    <% else %>
    <h4 class="headline"><%= fa_icon 'edit' %> Sign in</h4>
    <%= render 'devise/sessions/new' %>
  </div>
  <div class="holder">
    <h4 class="headline"><%= fa_icon 'book' %> Haven't signed up yet?</h4>
    <%= link_to 'Register now!', new_user_registration_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
<div class="col-sm-6">
  <div class="holder">
   <% if user_signed_in? %>
   <h4 class="headline"><%= fa_icon 'building-o' %> Featured Clubs</h4>

   <% else %>
   <h4 class="headline">.</h4>
   <% end %>
 </div>
</div>
<div class="col-sm-3">
  <% if user_signed_in? %>
  <h4 class="headline">What's Popular</h4>
  <% end %>
</div>

